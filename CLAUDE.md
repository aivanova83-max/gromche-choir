# CLAUDE.md — Хор «Громче»

## 1. Project Description

Публичный сайт женского хора «Громче» (Москва и МО) — лендинг/витрина с информацией о хоре, расписанием занятий и формой записи. Целевая аудитория: женщины, которые хотят петь в хоре без профессиональных требований.

---

## 2. Tech Stack

| Категория | Технология |
|---|---|
| Фреймворк | React 18 |
| Язык | TypeScript (non-strict: `strict: false`, `noImplicitAny: false`) |
| Сборщик | Vite + SWC (`@vitejs/plugin-react-swc`) |
| Стили | Tailwind CSS v3 + CSS-переменные |
| UI-компоненты | shadcn/ui (Radix UI под капотом) |
| Анимации | framer-motion |
| Роутинг | react-router-dom v6 |
| Состояние/запросы | TanStack Query v5 |
| Формы | react-hook-form + zod |
| Уведомления | sonner (toasts) |
| Иконки | lucide-react |
| Графики | recharts |
| Dev-платформа | Lovable (lovable-tagger в vite.config) |
| CRM/запись | MoyKlass (виджет, встроен в index.html) |
| Пакетный менеджер | npm (есть и bun.lock, но основной — npm) |

---

## 3. Project Structure

```
gromche-choir/
├── index.html              # Точка входа. SEO-метатеги на русском, og/twitter-теги,
│                           # скрипт виджета MoyKlass, подключение /src/main.tsx
├── src/                    # Директория исходного кода (создаётся по мере разработки)
│   │                       # Ожидаемая схема (по компонентам shadcn):
│   ├── main.tsx            # Точка входа React
│   ├── App.tsx             # Корневой компонент, роутер
│   ├── index.css           # Глобальные стили, CSS-переменные Tailwind/shadcn
│   ├── components/
│   │   ├── ui/             # shadcn-компоненты (Button, Dialog, Input и т.д.) — не редактировать вручную
│   │   └── ...             # Кастомные компоненты проекта
│   ├── pages/              # Страницы приложения (см. маршруты ниже)
│   ├── hooks/              # Кастомные React-хуки (алиас @/hooks)
│   └── lib/
│       └── utils.ts        # cn() и утилиты (алиас @/lib)
├── tailwind.config.ts      # Кастомная цветовая палитра, анимации, тени
├── vite.config.ts          # Алиас @/ → src/, порт 8080, lovable-tagger в dev
├── components.json         # Конфиг shadcn/ui
├── tsconfig.app.json       # TS-конфиг для src/
├── package.json
└── CLAUDE.md               # Этот файл
```

**Алиас импортов:** `@/` → `./src/`. Всегда использовать `@/` вместо относительных путей через несколько уровней.

---

## 4. Conventions

### Коммиты
Проект создан через Lovable — коммиты от Lovable идут в свободном формате. Для ручных правок:
```
feat: добавить секцию расписания
fix: исправить ширину карточки на мобиле
style: выровнять отступы в Hero
chore: обновить зависимости
docs: обновить CLAUDE.md, добавить описание маршрутов
```
- Язык: русский
- Тело коммита: опционально, описывает «почему», а не «что»

### Именование файлов и компонентов
- Компоненты: `PascalCase.tsx` → `HeroSection.tsx`, `ScheduleCard.tsx`
- Хуки: `camelCase` с префиксом `use` → `useSchedule.ts`
- Утилиты/хелперы: `camelCase.ts`
- Страницы: `PascalCase.tsx` в `src/pages/`
- CSS-переменные: `--kebab-case` (уже заведены в tailwind.config.ts)

### Ветки
- Основная: `main`
- Разработка: `claude/init-session-pEhXe` (текущая сессия)
- Паттерн для фич: `feat/<название>` или `fix/<название>`

### Стили
- Использовать **только Tailwind-классы** и кастомные CSS-переменные из `tailwind.config.ts`
- Кастомная палитра: `olive`, `warm-cream`, `warm-peach`, `warm-text`, `warm-bg`
- Кастомные градиенты: `bg-gradient-warm`, `bg-gradient-hero`
- Кастомные тени: `shadow-soft`, `shadow-warm`
- Кастомные анимации: `animate-fade-in`, `animate-float`, `animate-pulse-subtle`
- Dark mode — не используется (конфиг `darkMode: ["class"]` есть в tailwind, но тёмная тема на сайте не применяется)

---

## 5. Environment Variables

Переменные окружения ещё не определены. По мере добавления интеграций — вносить сюда:

```env
# [заполни сам] Если ID виджета MoyKlass будет вынесен из index.html:
# VITE_MOYKLASS_WIDGET_ID=

# [заполни сам] Если добавится бэкенд или CMS:
# VITE_API_URL=

# [заполни сам] Если добавится аналитика (Яндекс.Метрика и т.д.):
# VITE_METRIKA_ID=
```

> Все переменные, доступные в браузере, должны начинаться с `VITE_`.  
> Никогда не коммитить файл `.env` — добавить в `.gitignore`.

**Сейчас:** ID виджета MoyKlass (`01K9BrWJck7O6TZEZsOjLHe61iGjhIOYkjgp`) захардкожен в `index.html` строка 36. При необходимости вынести в переменную окружения.

---

## 6. What Claude Should Know

### Бизнес-контекст
- Сайт для **женского хора** без профессионального ценза — тон тёплый, поддерживающий, не академический
- Слоган: «Ты можешь громче!»
- Главный CTA — **запись на занятие** через виджет MoyKlass (уже вставлен в `index.html` как `<div id="SiteWidgetMoyklass133029">`)
- Аудитория — женщины любого возраста, не обязательно с музыкальным образованием

### Технические особенности
- **Lovable-платформа**: в dev-режиме работает `lovable-tagger` — добавляет `data-component-id` атрибуты к компонентам для визуального редактора. Это нормально, не удалять
- **TypeScript non-strict**: проект намеренно использует мягкие TS-настройки (совместимость с Lovable-генерацией). Не ужесточать без необходимости
- **shadcn/ui**: компоненты в `src/components/ui/` генерируются через CLI (`npx shadcn@latest add ...`). Редактировать их напрямую нежелательно — изменения могут перезатереться
- **Dev-порт**: 8080 (не стандартный 5173)
- **MoyKlass**: внешний виджет записи на занятия. Логика записи — на стороне MoyKlass, не дублировать её в коде

### Дизайн-система
- Вся цветовая схема через CSS-переменные — цвета в `tailwind.config.ts` ссылаются на `hsl(var(--...))`. Значения переменных должны быть в `src/index.css`
| Роль | HSL | HEX |
|---|---|---|
| Background (фон) | `hsl(40, 40%, 96%)` | `#F7F0E8` |
| Foreground (текст) | `hsl(15, 20%, 25%)` | `#4D3B32` |
| Primary (основной) | `hsl(15, 70%, 73%)` | `#E8A088` |
| Primary Foreground (текст кнопок) | `hsl(0, 0%, 100%)` | `#FFFFFF` |
| Secondary | `hsl(30, 65%, 80%)` | `#E4C9A8` |
| Secondary Foreground | `hsl(15, 20%, 25%)` | `#4D3B32` |
| Muted | `hsl(350, 60%, 85%)` | `#E8B0B8` |
| Muted Foreground | `hsl(15, 16%, 46%)` | `#886E63` |
| Accent (золотой) | `hsl(45, 75%, 65%)` | `#D4B44D` |
| Accent Foreground | `hsl(15, 20%, 25%)` | `#4D3B32` |
| Destructive | `hsl(0, 84%, 60%)` | `#EF4444` |
| Border / Input | `hsl(30, 30%, 88%)` | `#E8DDD0` |
| Ring | `hsl(15, 70%, 73%)` | `#E8A088` |
| Olive | `hsl(65, 25%, 60%)` | `#A3A876` |
| Card | `hsl(0, 0%, 100%)` | `#FFFFFF` |
| Warm Cream | `hsl(35, 50%, 92%)` | `#F2E8D8` |
| Warm Peach | `hsl(15, 70%, 73%)` | `#E8A088` |
| Warm Text | `hsl(20, 25%, 35%)` | `#6E5040` |
| Warm BG | `hsl(30, 60%, 95%)` | `#FAF2E8` |

**Градиенты:**
- Hero / кнопка: `linear-gradient(135deg, #E8A088, #E4C9A8)`
- Warm фон: тот же градиент с 20% прозрачностью

### Подсказки для работы с проектом
- **MoyKlass живёт вне React.** Виджет рендерится в `<div id="SiteWidgetMoyklass133029">`, который находится в `<body>` до `<div id="root">`. Управлять им через React-состояние невозможно — это внешний скрипт. Страница `/zapis` просто показывает этот div, не оборачивая его в компоненты.
- **`/events/mother` — статическая страница, не динамический маршрут.** Это не `/events/:slug`, а отдельный файл под конкретное прошедшее мероприятие. При добавлении нового события создавать новый файл по паттерну `EventИмяМероприятия.tsx` и отдельный маршрут, пока не появится CMS или источник данных.
- **Цвета задавать только через CSS-токены, не через HEX напрямую.** Таблица в разделе «Дизайн-система» — справочник для понимания палитры. В коде использовать исключительно Tailwind-классы (`bg-primary`, `text-muted-foreground`) или CSS-переменные (`hsl(var(--primary))`). Прямой HEX в JSX/CSS — ошибка.

### Страницы / маршруты
| Маршрут | Файл | Описание |
|---|---|---|
| `/` | `pages/Index.tsx` | Главная страница (лендинг) |
| `/events` | `pages/Events.tsx` | Афиша мероприятий |
| `/events/mother` | `pages/EventMother.tsx` | Страница прошедшего мероприятия |
| `/zapis` | `pages/Zapis.tsx` | Запись на занятие — виджет MoyKlass |
| `/legal` | `pages/Legal.tsx` | Перечень юридических документов |

> **Замечание:** страница `/events/mother` — пример страницы конкретного мероприятия. По мере появления новых событий паттерн именования: `/events/<slug>`.

---

## 7. Don't

1. **Не трогать `src/components/ui/`** — это shadcn-компоненты. Кастомизация только через CSS-переменные в `index.css` или обёртки поверх них.
2. **Не убирать `lovable-tagger`** из `vite.config.ts` и не удалять `data-component-id` атрибуты из JSX — они нужны для интеграции с платформой Lovable.
3. **Не менять порт** dev-сервера с 8080 — Lovable ожидает именно этот порт для превью.
4. **Не ужесточать TypeScript** (`strict: true`, `noImplicitAny: true`) без согласования — это сломает совместимость с кодом, генерируемым Lovable.
5. **Не хардкодить тексты и цвета** напрямую в компонентах — использовать CSS-переменные для цветов и константы/переводы для текста.
6. **Не коммитить `.env`** файлы — чувствительные данные только в переменных окружения, в репозитории только `.env.example`.
7. **Не хардкодить API-ключи** — любые ключи и секреты только через `.env`-переменные с префиксом `VITE_`.
8. **Не пушить в `main` напрямую** — все изменения через pull request; ветка `main` защищена.
9. **Не удалять файлы миграций БД** — если появятся, они фиксируют историю схемы и необходимы для воспроизводимых деплоев.
10. **Не коммитить `node_modules`, `.env` и другие секреты** — убедиться, что они перечислены в `.gitignore` перед первым коммитом.
11. **Не смешивать `bun` и `npm`** — основной пакетный менеджер `npm`. Использование `bun install` / `bun add` обновит `bun.lock`, но не `package-lock.json`, которым пользуется Lovable при деплое. Версии разойдутся.
12. **Не добавлять inline-стили (`style={{}}`)** — всё через Tailwind-классы и CSS-переменные из `tailwind.config.ts`. Inline-стили перебивают токены дизайн-системы и не обновляются при смене переменной.
13. **Не создавать собственную форму записи** — запись на занятия только через виджет MoyKlass. Расписание, свободные места и оплата живут на стороне MoyKlass; дублирование этой логики гарантирует рассинхронизацию.
14. **Не использовать относительные пути в импортах** — только алиас `@/`. Путь вида `../../components/Button` ломается при переносе файла; `@/components/Button` работает всегда.
15. **Не заменять внешние CDN-ссылки в `og:image` / `twitter:image` на локальные пути** — мета-теги в `index.html` не проходят через Vite-сборку, локальный путь не резолвится, и превью в соцсетях перестаёт работать.
